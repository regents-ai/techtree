# Techtree handoff, 2026-09-19

> Historical (2026-09-25): superseded by [the 0.3.0 plan](plan/v0.3.0-skill-environments.md) and [release compatibility](../README.md#release-compatibility). Kept as a record.

Written the day after v0.2.0 shipped. This is the single document a new
engineer or agent should read before touching Techtree: what the stack actually
is, what is live right now with its exact coordinates, what v0.2.x and v0.3.0
are meant to contain, and how the EvoSkill v2 codebase relates to that work.

Binding sources, in order of authority: `cli/docs/decisions/` (rulings),
`docs/v0.2/DECISION_LEDGER.md`, `docs/plan/*.md`, then this document. When this
document and a decision disagree, the decision wins.

Standing rules that stay in force for whoever works here next: every push,
merge to `main`, deploy, production data change, Fly secret or config change,
and contract or money action needs the founder's explicit word for that exact
action; hard cutover always (no fallbacks, compatibility branches, shims,
aliases or dual shapes); one writer per repository; never read `.env` files;
never print secrets or connection strings; never drop or recreate a database
without an explicit grant; never write tests unless directed; never run
bd/beads; never bare `git stash`; never `rm -rf`.

## 1. The stack, as it really is

One repository, `regents-ai/techtree`, holds three deliverables that release
together. A second repository, `regents-ai/techtree-hermes`, is a pure export
of `plugin/` so Hermes can install it by commit.

### Website and hub: `platform/`

| Layer | What is actually used |
| --- | --- |
| Language | Elixir `~> 1.15` (built on `hexpm/elixir:1.19.5-erlang-28.3.1-debian-trixie-20260610-slim`, runs on `debian:trixie`) |
| Web | Phoenix 1.8.13, Phoenix LiveView 1.2.11, Bandit 1.12.5 |
| Domain | Ash 3.33.6 (`~> 3.33.4`, includes the field-policy security fix), ash_postgres 2.13.0, ash_phoenix `~> 2.0` |
| Database | PostgreSQL 17. Production is the shared `regents_prod` database, schema `techtree_app`. Local development is PostgreSQL 17 on 5432 (ignore the `postgresql@14` install). |
| Hosting | Fly app `techtree-sh`, one machine `2879091f76d458` in `iad`, 512 MB, `--ha=false`, release command `/app/bin/migrate`. Secrets are recorded by name only, e.g. `TECHTREE_BOOTSTRAP_CHANNEL=stable`. |
| Shared packages | `regent_ui` (design system, staged at `502d10b…`), `regent_privy_shared` and `regent_blog` (from `elixir-utils` `a12f8c8…`), `regent_identity` (from `regents` `f2c53b7…`). They are copied in with `mix regent_ui.stage`, `mix regent_identity.stage`, `mix regent_blog.stage`; never edit the staged copies. |
| Frontend | Server-rendered LiveView pages. esbuild bundles the vendored browser dependencies in `platform/assets/` (React 19 and Privy `react-auth` for sign-in, `vgpu`) into `priv/static/assets` so the strict `script-src 'self'` policy holds and nothing is fetched at runtime. Hand-written CSS in `platform/assets/css/app.css`; no Tailwind, no SPA. |
| Release data | `platform/priv/releases/<release_id>/{bootstrap.json,release-core.json,checksums.json}` and the immutable catalog tree `platform/priv/catalog/sources/<full-source-revision>/…`. Both are tracked in git. |

Deploy recipe: `platform/scripts/deploy_site.sh` (refuses a dirty tree or an
unpushed HEAD; it compares HEAD to `@{u}`), then
`Techtree.Release.import_catalog("<source-revision>")` on the machine to switch
the served catalog. `platform/docs/release/deploy-flyio.md` is the long form.

### CLI: `cli/`

| Layer | What is actually used |
| --- | --- |
| Language | Python `>=3.12,<3.14`, managed with `uv`, built with hatchling (the build stamps the source commit into the wheel) |
| Libraries | typer, pydantic 2, rich, cryptography, rfc8785 (canonical JSON), platformdirs, filelock |
| Commands | `doctor`, `setup`, `profile`, `climb`, `skill`, `run`, `engine`, `proof`, `release`, `uplift`, `forge build`, `forge status` |
| Local state | One root from platformdirs (`~/Library/Application Support/techtree` on macOS): `config.toml`, `cache/` (content-addressed Skills), `drafts/`, `runs/`, `engines/` (content-addressed engine installs), `identities/`, `forge/{env,builds}/` |
| Managed engine | Verifiers 0.3.1 pinned at `b2e4e8157783b2c0dffc7821044c87f29f1c3ccf` |
| Host agent | Hermes 0.20.1 (minimum and maximum tested); the subject agent for the public climb is Hermes 0.19.0 |
| Task generator | Repo2RLEnv, run inside Docker; the forge code lives in `cli/src/techtree/forge/` |
| Checks | `make check` (format, lint, typecheck, tests, generated-check, v0.2 preflight), `make test-integration`, `make check-plugin`, `make regenerate` |

Release identity for the CLI is `cli/src/techtree/resources/release/release-core.json`
verified by `cli/tools/verify_release_core.py`. Publishing is
`.github/workflows/publish-cli.yml` (inputs: tag and expected wheel sha256;
PyPI trusted publishing through the `pypi` environment; the publisher is
`regents-ai/techtree` + `publish-cli.yml` + env `pypi`; the old
`techtree-python/publish.yml` publisher has been removed).

### Hermes plugin: `plugin/`

`techtree-hermes` 0.2.0, no Python dependencies, one executable (`techtree`),
an environment allowlist, no network access of its own. Seventeen tools
(`techtree_bootstrap_check`, `techtree_bootstrap_install`,
`techtree_climb_list`, `techtree_climb_inspect`, `techtree_climb_prepare`,
`techtree_climb_start`, `techtree_demo_prepare`, `techtree_run_status`,
`techtree_run_result`, `techtree_run_cancel`, `techtree_proof_verify`,
`techtree_publish_run`, `techtree_system_check`, `techtree_uplift_context`,
`techtree_uplift_prepare`, `techtree_uplift_propose`,
`techtree_uplift_start`) and two hooks. The Hermes plugin scanner reports five
CAUTION findings (`guards.py:86`, three `subprocess.run` calls in `bridge.py`,
`channels.py:30`); they are expected, identical to the previous release, and
documented in `cli/release/hermes-scanner-dossier.md`. Install is by exact
commit, never by branch.

## 2. What is live right now (v0.2.0, published 2026-09-18)

### Coordinates

| Thing | Value |
| --- | --- |
| CLI source (tag `v0.2.0`, "X") | `70e75c7dd9a762b43d00b484abd432dc791ffa89` |
| Plugin export commit ("P") | `4567937f4d650df83820ccb2239e9bed0b98cddc` (tree `50edd311b2f161fd347472ecb2d2d38b53ef943e`) |
| Release metadata commit ("Y") | `29ceee00595ff317f7fb60a1712d7802d6620c8c` |
| `main` and the deployed site | `451f83615eaa4e73c880e9148cd2d42725fcf8d0` (Y plus the header star button) |
| Wheel `techtree-0.2.0-py3-none-any.whl` | sha256 `ad966130cf0279d869d45169de4559ba7dfba669818afde28107716a0b47b375`, 825063 bytes, on PyPI |
| ReleaseCore digest | `sha256:3b6e6c940719452605b04e206fc353f0ced1b46130b5a0cb282be1fe5a1f4f7e` |
| Catalog index digest | `sha256:4d2165719e1e90cf838d284112fa3c1935ebc4b03917c178725c6d4ba57b7eb5` |
| Bootstrap document digest | `sha256:e9170038c673755d4043e5a207f48bc1286315ba7412f2ec725c031d13deb195` |
| Fly image now serving | `deployment-01M2TXDDK1K9PJ30VCD1M84J57` (previous, for rollback: `deployment-01M26KYBPRTB5FBP3Z2HHAVR77`) |
| Retained previous catalog snapshot | source `614daffbcbd294be4646adfdec26f95337c4f7ed`, catalog `sha256:10a7fcc5…` |

Evidence for the whole release sits in
`artifacts/techtree-pairing/release-v020/` (rehearsal, deploy, plugin install,
PyPI, post-cutover object checks, release notes).

### What a person can do today

- Install the CLI from PyPI (`uv tool install --python 3.12 techtree==0.2.0`)
  and the Hermes plugin by commit; `techtree doctor` and
  `hermes plugins doctor techtree --ci` both pass on a clean machine.
- Run the public climb: the same pinned Hermes runs the same synthetic tasks
  twice, once with a declared Skill added, and a signed receipt records the
  difference. `techtree proof verify` checks that receipt offline.
- Publish the result to techtree.sh and browse Results by harness, model,
  exact agent version and exact challenge (an unknown selection shows nothing
  rather than unrelated results). The agent-version browser
  (`docs/plan/agent-version-browsing.md`; the "awaiting founder review" status
  line in that file is stale, the control is on the live `/results` page) groups
  submissions by agent family, defaults to the newest version with accepted
  submissions, keeps the selection in the URL, and never pools scores across
  versions or models. Results and objects from before v0.2.0 remain at their
  addresses; uploads made with CLI 0.1.1 against the earlier campaign are
  refused.
- Improve a Skill with `techtree uplift`: the plugin gathers the context of a
  finished run, proposes a revision, and starts the next measured run.
- Turn a local Git repository into repair tasks with `techtree forge build`.
  Each accepted task is proven both ways (the unrepaired code fails the
  relevant tests, the reference repair passes them) without calling a model,
  and `techtree forge status` shows retained builds, including failed or
  cancelled ones, honestly. Qualification was reproduced for one selected
  public repair, locally and on a fresh Linux worker.
- Read `/changelog`, `/docs`, `/start`, `/verify`, `/blog`, `/repo2rlenv`,
  `/skill.md`, and the machine surfaces
  `/api/v1/{healthz,bootstrap,catalog,climbs/:slug,objects/:digest,publications,publication-keys/:id}`.

### What is deliberately not live

`forge run`, `forge compare`, evaluation of the default or a named Hermes
profile with isolated state, Prime hosted evaluation, Fabric-backed
Hermes/Codex comparisons, Relay evidence, hosted comparisons, public
collaboration and payments, automatic Skill optimization, private proving, and
training. The changelog's "Not in this release" section is the customer-facing
statement of this list; keep it truthful.

## 3. v0.2.x: what we know we want

The order below is the recommended order, not a promise. Each item is
released only when it is qualified end to end, and the changelog says exactly
what was and was not exercised.

### 3.1 `forge run` and `forge compare` (the Repo2RLEnv local lane)

Plan: `docs/plan/repo2rlenv-local-lane.md`. The target workflow: a person
points Techtree at a repository on their own machine, Techtree builds proven
repair tasks (already live), then runs the person's own Hermes against those
tasks twice, as configured today and with a candidate Skill added. The
difference is private local evidence about whether the Skill helps that agent
on that codebase, and it feeds the existing `uplift` loop.

Facts the design rests on: forge artifacts stay local unless separately
approved for disclosure; the subject is the person's own Hermes calling its
own provider (so inference still leaves the machine under that provider's
policy); Techtree never copies a credential; a comparison is evidence about
one agent on one codebase, never a leaderboard.

Founder priority, 2026-09-19: **make v0.2.1 a complete, useful repository
experiment, not another framework.** The product sentence to keep in front
of whoever builds it: a developer can find out whether a particular Skill
helps their agent on their repository, understand the failures and costs,
and decide what to change next. The milestone that matters is someone other
than the founder using Techtree to decide whether to keep a Skill.

The 0.2.1 packet, in build order:

1. **Forge experiment contract and comparability gate.** A small Forge run
   specification on the existing pydantic and canonical-JSON infrastructure
   that binds: build and qualified-task membership (already committed in
   `forge/models.py`), task content and grading configuration, Hermes
   executable/version and selected configuration, requested provider/model
   and whatever identity evidence is available, initial-state policy, Skill
   exposure policy, baseline or candidate Skill identity, resource limits,
   sampling settings and repetition plan. Record what cannot be established
   (a matching model name is not proof of identical weights; an unavailable
   seed is not invented). Only the declared treatment (the Skill) may
   differ; the comparison reuses `manifests/compare.py`'s walk and does not
   force private local results into the public proof format merely to reuse
   a type.
2. **`forge run` with the user's local Hermes.** Before: validate the
   qualification and committed task contents, resolve the agent
   configuration, show provider egress and limits. During: separate task
   workspaces and arm state, no access to reference solutions or hidden
   grading material, progress retained if the terminal closes. After:
   collect permitted changes, grade under the approved contract, preserve
   outputs, report cleanup. Outcomes stay distinct: task failed,
   infrastructure failed, timed out, cancelled, evidence incomplete,
   completed. Never silently replay an ambiguous paid attempt; never reuse
   mutable workspace state between arms; unavailable dollar cost is not $0
   (record observed usage and durations, enforce only limits that can be
   enforced).
3. **`forge compare` with a local, self-contained HTML report** beside the
   machine-readable output, before any new dashboard server. First screen
   answers: what changed, did it help, where did it regress, what did it
   consume, how strong is the conclusion. Structure: repository and
   experiment identity; a "local, mutable-subject evidence" label; the
   question tested; a plain-language summary; baseline | candidate |
   difference with completion, usage, elapsed time; per-task wins, losses,
   ties and unresolved attempts; candidate patches and grading diagnostics;
   configuration differences and limitations. All planned tasks stay
   visible; missing pairs are shown as partial observations, never as a
   complete uplift result. Frontend stack as in section 1: shared Regent
   UI, hand-written CSS, no Tailwind, no SPA.
4. **One revision cycle** through the existing `uplift`: one explicit
   proposal, frozen, checked, approved, re-measured, and kept whether it
   improves or regresses. No autonomous search.
5. Qualify in Docker and on a fresh Linux worker the way the task pipeline
   was (Docker on the founder's machine was timing out on 2026-09-17), then
   publish 0.2.1 with an honest changelog entry.

Acceptance for the packet: real baseline/candidate attempts; correct refusal
of incompatible comparisons; useful task-level evidence; no credential
copying and no automatic publication; successful use outside the original
development environment. Excluded: autonomous epoch search, private
proving, new harnesses, training, hosted execution, payments, another
orchestration framework.

Open founder decisions blocking step 1 (asked in the 2026-09-18 brief, not
yet answered; state them with options and a recommendation every time they
are mentioned):

1. Grading of agent attempts. Options: (a) approve direct Harbor-compatible
   grading now, clearly labelled as such; (b) wait for a demonstrated
   Verifiers handoff. Recommendation: (a), so `forge run` is not blocked on an
   upstream integration; it can be replaced by hard cutover later.
2. Starting state for each arm. Options: (a) fresh empty state for both arms,
   memory off, recorded in the result; (b) each arm's own current state,
   recorded as-is. Recommendation: (a); it is the only version that gives a
   clean comparison, and it is what EvoSkill's freeze step does too.
3. Paid model use. Options: (a) the product uses the end user's own account
   only, and my qualification runs get a fresh small cap (about USD 10, as the
   earlier Prime cap was, of which USD 0.026 was spent); (b) the old cap is
   reused. Recommendation: (a).

### 3.2 After 0.2.1: external-user reproduction, then the Prime handoff

Only after `forge run` and `forge compare` are qualified: another developer
completes the workflow on their own machine, and one explicitly admitted
Prime path consumes the environment and returns usable evidence. Two Prime
deliverables stay distinct: *reference interoperability* (the generated
package runs through a specific Prime/Verifiers configuration) and *managed
hosted evaluations* (create, monitor, cancel, retrieve, reconcile remote
jobs), and the second stays blocked as section 3.3 says. Prime's local
`prime eval` uploads by default unless `--skip-upload` is passed; local
privacy has to be deliberate.

### 3.3 Prime Hosted Evaluations (WP4)

Blocked on upstream contract gaps recorded in
`docs/v0.2/UPSTREAM_CONTRACT_LOCK.json`; the lock itself awaits founder
decision 16 in the decision ledger. Do not start implementation until the
lock is signed off.

### 3.4 Techtree Market proof pilots (`techtree-33x`)

Plan: `docs/plan/techtree-market.md`. Market code stays inside `platform/`;
the CLI and plugin remain fully usable without it. Slices, in order:

- `33x.1` independent reproduction linkage (a rerun by the same participant is
  not independent);
- `33x.2` signed, canonical `techtree.market.*` records for `BountySpec`,
  `Submission`, `AcceptanceDecision`, append-only;
- `33x.3` payee-control assertion, `PayoutIntent`, observed Safe execution
  reconciled into a `PayoutReceipt` (Techtree never initiates value transfer);
- `33x.4` Techtree Library: listings backed by verified proofs and declared
  source rights, evidence facets preserved, never a leaderboard;
- `33x.5` the x402 access contract, locked before any code;
- `33x.6` the authenticated agent API (authorization, request envelopes,
  replay protection, idempotency, rate and size bounds, audit trail).

The Proof Relay parameters (operators, cap, amount, chain, token, Safe,
signers, ceiling, challenge window, acceptance authority, rollback rules) are a
protected founder packet, and the mandatory security gates in that plan block
any pilot outright rather than downgrading it to a weaker label.

### 3.5 Not in v0.2.x

Marketplace beyond the pilots above, Foundry, private proving, training and
extra harnesses stay out, per the founder's 2026-09-17 scope.

## 4. v0.3.0: Techtree Foundry and the first Skill Climb (`techtree-8dj`)

Foundry turns a source bundle into a versioned, isolated environment whose
tasks and verifier are kept apart from the agent under test, and records every
step as a receipt. Slices:

- `8dj.1` source rights: `SourceBundle` binds origin, digest, rights,
  permitted uses, confidentiality and lineage; import refuses missing or
  contradictory rights;
- `8dj.2` environment compilation into a versioned `EnvironmentBlueprint` with
  a receipt binding tools, dependencies, network policy and task material;
- `8dj.3` environment and verifier separation: subjects can never read
  verifier code, answers, held-out data or credentials, and failing the
  declared isolation strength blocks the proving use;
- `8dj.4` explicit development / validation / proving memberships,
  digest-bound and auditable, with receipts stating the real isolation
  strength;
- `8dj.5` validation receipts for environment builds and task sets;
- `8dj.6` search lineage: candidate lineage and search receipts binding parent
  candidates, allowed mutation, prompts or programs, budgets, observed
  development evidence and selection decisions, never proving answers;
- `8dj.7` the first private Skill Climb, blocked by the Market signing, payee,
  authorization and reconciliation contracts;
- `8dj.8` private and executable artifact admission policies;
- `8dj.9` the customer Foundry pilot, plus the narrower approved Repo2RLEnv
  customer service that needs its own qualified recipe but not positive
  uplift.

Separate private proving (the customer's held-out set never leaves their
boundary) ships with Foundry, not before. The v0.3.x studies (`techtree-5t7`:
Prime Agent, prime-rl, model uplift, harness artifacts, Fabric Harness Climb,
Relay cross-harness metrics, environment quality and utility) are deferred
until Foundry and Skill Climb evidence exists.

## 5. EvoSkill v2 and how it can help us directly

Repository: `https://github.com/sentient-agi/EvoSkill/tree/v2` (Apache-2.0,
Python 3.12+, uv, Docker; about 1200 stars; last pushed 2026-08-24). I read
the README, the layout and the module names; I did not run it.

### 5.1 What EvoSkill v2 is, in our terms

EvoSkill evolves a Skill (a markdown tree, like ours) for a fixed agent
against a benchmark, using three agents: a solver (the subject, run through
OpenRouter), an evolver (edits the Skill, run through a Claude Code
subscription with `ANTHROPIC_API_KEY` stripped by `subscription_env()`), and
an agent orchestrator. One epoch is: freeze the instrument, roll out the
solver over three samples, evolve using training digests only, measure on a
validation split and promote every edit, then run the held-out test once.
Benchmarks are τ³-bench banking and SpreadsheetBench v2. Published results:
SpreadsheetBench debugging went from 3 to 21 of 120; τ³ showed no separation;
a three-epoch evolution costs about USD 27, about USD 0.025 per episode.

The parts that matter to us, with where they live:

| EvoSkill piece | Where | Techtree equivalent today |
| --- | --- | --- |
| Comparability gate `compare_runs()`: fifteen instrument fields plus split hash, sealed-test flag and sample count must match before two runs may be compared | `src/orchestrator/comparability.py` | Already stronger in Techtree: `manifests/compare.py` diffs the two variants' canonical bytes and refuses any difference outside the Campaign's mutation contract (and refuses no difference at all); `compatibility.py` does the same for reproductions under a declared drift policy. `forge compare` must reuse these, not add a third comparator. |
| Freeze → rollout → evolve → measure → test-once epoch loop | `src/orchestrator/loop.py`, `evolver_dispatch.py` | `uplift` (one propose-and-measure step, no loop), and the v0.3 `8dj.6` search lineage design. |
| Training / validation / held-out split with a sealed test set | `spec.py`, `brief.py` | v0.3 `8dj.4` memberships. Not in v0.2. |
| Content-addressed replay cache | `src/cache/` | Content-addressed catalog, Skill cache and engine installs (`paths.py`), but no episode replay cache. |
| Evolver containment via macOS `sandbox-exec`, Linux only by recorded opt-out | orchestrator | Techtree runs task material inside Docker on every platform (`forge/docker.py`); Techtree's boundary is stronger and portable. |
| Human gates with sha256 receipts | orchestrator | Signed receipts and approvals; same idea, ours are signed rather than hashed. |
| Leak patterns and Skill integrity checks | `leak_patterns.py`, `skill_integrity.py` | Integrity: every Skill is content-addressed and re-verified file by file (`skills/archive.py`). Leaks: the plugin refuses answer-table *shapes* in a proposal (`plugin/cli/guards.py`); since 2026-09-19 the CLI also refuses any candidate Skill that contains a proving input of the reference taskset, on both preparation paths (`skills/service.py`, `uplift/public_tasks.py`). |
| Phoenix (Arize) tracing | `tracing.py` | None; we retain raw logs and validation output per task. |
| Quota scoping per run | `quota_scope.py` | Open decision 3 above. |

### 5.2 Ideas to take (ordered by value to the v0.2.x lane)

1. **Keep one comparator, and make `forge compare` use it.** Techtree
   already has what EvoSkill's comparability gate does, and more strictly:
   `manifests/compare.py` walks the two variants' canonical bytes and refuses
   any difference outside the Campaign's mutation contract. `forge compare`
   must describe its two arms as a Campaign and two manifests and go through
   that check; the only new work is the forge-specific fields (repository
   revision, task-set digest from the build, Hermes profile and provider,
   seeds) becoming part of the Campaign so the existing walk covers them. Use
   `comparability.py`'s field list only as a checklist for what the forge
   Campaign must carry.

2. **Leak checks on candidate Skills.** Done for the reference taskset on
   2026-09-19: `climb prepare` and `uplift prepare` both refuse a Skill that
   contains any of the taskset's proving inputs (case-insensitive whole
   words), with the offending file and words in the refusal. Unknown tasksets
   are not scanned, by the same lookup-not-inference rule as decision R1.
   For `forge` tasks the rule is different, because a repository path or a
   test name can be legitimate context: leakage detection there is
   evidence-based screening, not a hard refusal. Screen a candidate for the
   reference patch and test bodies of the build it was measured on,
   distinguish suspected answer contamination from ordinary terminology,
   and record the finding in the report so a reader can judge it. A scanner
   is never a substitute for keeping the solver workspace, the
   candidate-authoring inputs and the reference material separate. Both
   forms become a mandatory gate for `8dj.6`.

3. **Protected settings, declared treatment, unverifiable facts.** The most
   useful thing in EvoSkill's `comparability.py` is not its field list but
   its three-way split: fields that must match (refused when missing, not
   defaulted), the one declared treatment that may differ, and limitations
   the recorded artifacts cannot settle, named rather than assumed. The
   Forge experiment contract (section 3.1, item 1) adopts that split. Match
   the fixed configuration, permit the declared Skill difference, verify
   each arm's Skill integrity separately.

4. **Held-out is defined by information access, not by run count.** A
   result may call an evaluation "held-out" when its tasks were declared
   before any candidate was chosen and none of its feedback was used to
   revise or select a candidate. Repeated samples are fine; using the
   feedback is not. That is a claim rule on the receipt and the site, not a
   subsystem.

5. **Usage and cost per attempt.** Record observed tokens, requests and
   elapsed time per task and per arm, and dollar cost only when the
   provider states it; an unavailable cost is reported as unavailable, never
   as zero. This is what lets a spend limit be enforced where it can be and
   reported truthfully where it cannot.

Deferred, deliberately: an automated epoch loop (freeze, roll out, evolve,
measure, test) and search lineage belong to v0.3 (`8dj.6`), not 0.2.x; a
rollout replay cache is not needed because `forge compare` reads stored
results without inference, and a replayed attempt is not a new sample.

### 5.3 What we can offer EvoSkill, and where the code could join

- **Techtree as the frozen instrument and signed publication layer.**
  EvoSkill hashes its gates; Techtree signs whole receipts and verifies them
  offline. An EvoSkill epoch record could be wrapped as a Techtree run so the
  before/after evidence is bound to a key and checkable offline. A signature
  binds evidence to a signer; it does not establish honest execution or
  independent reproduction, and Techtree's Result labels say exactly that.
  Concretely: a small exporter in EvoSkill that emits a Techtree
  `CampaignSpec` plus per-episode results in our canonical JSON, and
  `techtree proof verify` on the other end. No changes to our schemas are
  needed for the public climb shape; a new campaign kind would be needed for
  τ³ or SpreadsheetBench tasks.
- **Portable containment.** EvoSkill's evolver sandbox is macOS-only. Our
  Docker-based task runner (`cli/src/techtree/forge/docker.py`) and the v0.3
  environment/verifier separation give a Linux answer. If they want it, the
  runner is the piece to extract, not the CLI.
- **Repo2RLEnv tasks as an EvoSkill benchmark.** `forge build` produces
  proven repair tasks with a control and reference result for each. Those are
  a ready-made, verifiable benchmark for their solver, one where the
  "did the Skill help" signal is a real test suite rather than a rubric.

### 5.4 What not to take

- Do not vendor EvoSkill. The licence allows it, but their orchestrator
  assumes a Claude Code subscription for the evolver, OpenRouter for the
  solver and Phoenix for tracing; none of that fits our "subject is the
  person's own Hermes" rule or our no-credential-copy rule.
- Do not import their "promote every edit" policy as-is; in our model a
  promotion is a lineage receipt that a human or an explicit policy signs.
- Do not present any EvoSkill-derived loop as qualified until it has been
  reproduced end to end under our own rules; their τ³ result showing no
  separation is a reminder that the honest outcome is often "no effect".

### 5.5 First slice, and what it turned out to be

The founder approved starting with items 1 and 2 of section 5.2 on
2026-09-19. Reading the code showed item 1 already exists (see the table),
so nothing was built for it. Item 2 was built for the reference taskset and
verified against the live Hello World Climb: a clean starter Skill and a
Skill containing only look-alike words (`Cedarwood`) prepare normally; a
Skill with an answer table naming `Cedar` and `alder` is refused with code
`candidate_policy_violation`. No test was added, per the standing rule that
tests are written only when directed; the existing preparation suites pass.
Next: the 0.2.1 packet in section 3.1, starting with the Forge experiment
contract, once decisions 1 to 3 are resolved.

## 6. Where things are

- Release worktree: `worktrees/techtree-release-v020` (branch
  `release/v0.2.0`, fully merged into `main`).
- Plugin export worktree: `worktrees/techtree-plugin-v020`.
- Lane notes: `artifacts/techtree-pairing/notes-fable-2026-09-18.md`.
- Previous handoff: `artifacts/techtree-pairing/techtree-fable-5.1-handoff-2026-09-18.md`.
- Local rehearsal databases `techtree_release_v020` and
  `techtree_release_v020_final` were dropped on the founder's grant; the
  `.claude/launch.json` entry `techtree-release-v020-4063` needs a database
  created again before reuse.
