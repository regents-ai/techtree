# Current dependency environment for local agent compatibility checks.
# The published prime_environment package and its attested lock stay immutable.
[project]
name = "techtree-local-agent-checks"
version = "0.1.0"
requires-python = ">=3.12,<3.14"
dependencies = ["verifiers==0.3.1"]

[tool.uv]
package = false
